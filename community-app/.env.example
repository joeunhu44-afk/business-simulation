# ==============================
# 필수
# ==============================

# 세션(JWT) 서명용 비밀키. 아무 긴 무작위 문자열이나 사용.
# 생성 예시: openssl rand -hex 32
JWT_SECRET=

# MySQL 접속 문자열 (예: Railway MySQL 플러그인이 자동으로 채워줌)
DATABASE_URL=mysql://user:password@host:3306/dbname

# 배포된 앱의 실제 주소 (뒤에 슬래시 없이). 로컬 개발 시에는 http://localhost:3000
APP_URL=https://mydomain.com

# 이 이메일로 회원가입/로그인하면 자동으로 admin 권한이 부여됨
OWNER_EMAIL=me@example.com

# ==============================
# 소셜 로그인 (사용할 것만 채우면 됨 — 비워두면 해당 버튼 클릭 시 에러 응답)
# ==============================

# Google Cloud Console > API 및 서비스 > 사용자 인증 정보에서 발급
# 승인된 리디렉션 URI에 반드시 추가: {APP_URL}/api/auth/google/callback
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# 카카오 개발자 콘솔(developers.kakao.com) > 내 애플리케이션 > 앱 키
# Redirect URI에 반드시 추가: {APP_URL}/api/auth/kakao/callback
# "카카오 로그인 > 보안 > Client Secret"을 활성화했다면 아래도 채울 것 (선택)
KAKAO_CLIENT_ID=
KAKAO_CLIENT_SECRET=

# Apple Developer > Certificates, Identifiers & Profiles
# APPLE_CLIENT_ID: Services ID (예: com.mydomain.app.web)
# APPLE_TEAM_ID: 계정 팀 ID
# APPLE_KEY_ID: "Sign in with Apple" 용으로 만든 키의 Key ID
# APPLE_PRIVATE_KEY: 다운로드한 .p8 파일 내용 전체 (줄바꿈은 \n 으로 이어써도 됨)
# Return URL에 반드시 추가: {APP_URL}/api/auth/apple/callback
APPLE_CLIENT_ID=
APPLE_TEAM_ID=
APPLE_KEY_ID=
APPLE_PRIVATE_KEY=

# ==============================
# 파일/이미지 스토리지 (Cloudflare R2 권장 — S3 호환이면 무엇이든 가능)
# ==============================

# R2 예시: https://<account_id>.r2.cloudflarestorage.com
S3_ENDPOINT=
S3_REGION=auto
S3_BUCKET=
S3_ACCESS_KEY_ID=
S3_SECRET_ACCESS_KEY=
# 업로드된 파일을 실제로 열람할 때 쓸 공개 URL (R2 Public Development URL 또는 연결한 커스텀 도메인)
S3_PUBLIC_URL=

# 가입 자동 승인 도메인 (선택). 비워두면 모든 신규 가입이 관리자 승인을 거친다.
# 학교가 재학생 메일 계정을 발급한다면 쉼표로 나열: "school.hs.kr,second.ac.kr"
# 주의: 카카오·애플은 이메일을 안 주는 경우가 있어 이 방식만으로는 전원을 덮지 못한다.
AUTO_APPROVE_EMAIL_DOMAINS=

# (주의) 업로드 파일은 앱을 거쳐야만 열립니다. R2를 쓴다면 버킷을 비공개로 두세요.

# ==============================
# 업로드 파일 보관 위치 (★ 재배포해도 사진이 남으려면 반드시 확인)
# ==============================

# S3/R2를 설정하지 않으면 업로드 파일은 서버의 디스크에 저장됩니다.
# 그런데 Railway 같은 곳은 배포할 때마다 컨테이너를 새로 만들기 때문에,
# 일반 디스크에 저장하면 배포 한 번에 프로필 사진과 게시물 사진이 전부 사라집니다.
# (DB는 별도 서비스라 글·계정은 그대로 남고, 사진만 깨져 보입니다.)
#
# 해결책은 둘 중 하나입니다.
#   1) Railway에 Volume(영구 디스크)을 붙인다  ← 가장 간단, 추가 계정 불필요
#      Volume을 붙이면 RAILWAY_VOLUME_MOUNT_PATH가 자동으로 채워지고,
#      앱이 그 경로를 알아서 사용하므로 UPLOAD_DIR은 비워두면 됩니다.
#   2) 위의 S3_* 값을 채워 R2/S3를 사용한다
#
# 둘 다 안 되어 있으면 서버 부팅 로그와 관리자 패널 상단에 경고가 표시됩니다.
# 직접 경로를 지정하고 싶을 때만 아래를 채우세요 (Volume 마운트 경로와 같아야 합니다).
UPLOAD_DIR=
